# progress

## 2026-05-25
- カードポップアップの下端位置固定を実寸基準へ変更
  - 固定想定高さではなく、実際のポップアップ高さを測って下端をカード高の上2:下8境界へ合わせるよう修正
  - エンチャント詳細表示で高さが変わっても同じ基準へ追従
  - `apps/web/src/App.tsx`

- カード一覧の参照ポップアップ重なり順を修正
  - 一覧カードのホバー中だけ前面へ出すようにして、参照ポップアップが他カードの下へ埋もれないよう調整
  - `apps/web/src/styles.css`
- カード一覧の説明文表示をバトル中と統一
  - カード一覧でも変動数値に白背景プレートを表示するように修正
  - `『カード名 / エンチャント名』` にカーソルを合わせると、一覧カード下に詳細ポップアップが出るように修正
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`

- カード一覧の参照ポップアップ表示を安定化
  - 一覧側の参照ポップアップは、ホバー中の名前にだけ内包表示する方式へ変更
  - 点滅しやすかった外側共有ポップアップを廃止
  - 一覧側ではエンチャント名など参照語の文字色を黒へ調整
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`

## 2026-05-22
- 後生成カードの同ラウンド発動条件を調整
  - ラウンド中に後から生成されたカードは、現在の発動中カードより左側へ生成された場合、そのラウンドは発動しないように修正
  - `バブル` の左側生成が同ラウンドで発動しない回帰テストを追加
  - `packages/engine/src/index.ts`
  - `packages/engine/src/index.test.ts`

- カードポップアップ説明文の重複対策
  - ホバー説明文はカードインスタンス側ではなく定義側の元テキストを優先して描画
  - カード切替と参照詳細切替ごとにポップアップを再初期化するキーを追加
  - `apps/web/src/App.tsx`

- カードポップアップ位置を再調整
  - 左右の距離は元に戻し、上下だけ上寄りへ移動
  - カードポップアップの下端がカード高の上2:下8境界付近へ来るように変更
  - `apps/web/src/App.tsx`

- カードポップアップ位置を右上寄りに調整
  - カード真横で手札や場の次カードへ干渉しやすかったため、カード右上寄りへオフセットを変更
  - `apps/web/src/App.tsx`

- カード説明文中の参照語ホバーを追加
  - `『...』` で記述されたカード名・エンチャント名にホバーすると、カードポップアップ下へ詳細ポップアップが出るように修正
  - 付与エンチャント一覧の詳細ポップアップも同じ下段表示へ統一
  - 説明文中の参照語に仮の背景プレートを追加
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`

- 変動数値バインドを複数カードへ拡張
  - `オーバーチャージ` を除く未対応候補カードへ `textValueBindings` を追加
  - 加算値や倍率値を、通常時から白背景プレート付きで判別しやすくなるよう対象範囲を拡張
  - `packages/shared/data/cards/*.json`

- エンチャント詳細ポップアップの表示を修正
  - カードポップアップ内のエンチャント詳細が親の `overflow` で切られていたため、下段ポップアップが見えるよう修正
  - `apps/web/src/styles.css`

- `通電ショック` の説明文と可変値バインドを修正
  - `設置：` / `発動：` の改行前提に合わせて全角コロンへ統一
  - `発動：通常攻撃＋10` の `+10` を可変値バインド対象として追加
  - `packages/shared/data/cards/thunder_shock.json`

- カードホバーポップアップの縦位置を調整
  - カード右側に出るホバーポップアップの表示位置を、中央寄りから少し上寄りへ変更
  - `apps/web/src/App.tsx`

- プレイヤーブロック内ポップアップと高さを再調整
  - 属性出現率ポップアップとラウンドバフポップアップが見切れないよう、関連ブロックの `overflow` を見直し
  - ステータス数値が切れにくいよう、プレイヤーブロックの固定高とステータス枠の最小高さを再調整
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- カードホバーポップアップの配置と詳細表示を調整
  - カードにカーソルを合わせたときのポップアップを、カーソル追従ではなくカード右上寄りの固定位置へ表示する形に変更
  - 付与エンチャント名にカーソルを合わせると、その詳細ポップアップがカードポップアップの下に出るように調整
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- 変動対象の値を常時プレート表示に調整
  - カード説明文で、将来的に変動しうる値は普段から白い背景プレート付きで表示するよう変更
  - 実際に値が変動しているときだけ、引き続き赤字で強調する形に調整
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- 可変数値の表示を見やすく調整
  - カード説明文中で変動する値は、白い背景プレートの上に表示するよう変更
  - `+`、`×`、`倍` も数値と同じまとまりで表示するよう調整
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- `コールドスリープ` の表記を修正
  - 次ラウンド開始時まで待機する効果内容に合わせて、カードテキストを `設置` 表記へ変更
  - `packages/shared/data/cards/enchant_cold_sleep.json`

- フロント側の入力制限と表示安全性を調整
  - カード一覧の検索入力欄で、特殊記号を受け付けず、英数字・日本語・空白のみ入力できるように変更
  - ログ見出しの整形に使う正規表現は、タイトル文字列をエスケープして扱うように調整
  - 既存の CSP 設定はそのまま維持
  - `apps/web/src/App.tsx`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- プレイヤーブロックの上段高さを修正
  - ステータス下段がプレイヤーブロック外へはみ出していたため、プレイヤーブロック内の文字やバフ列を詰めたうえで固定高に再調整
  - プレイヤー名、ロール名、ラウンドバフ、ラウンド数、ステータス6枠が同じプレイヤーブロック内へ収まるよう修正
  - 名前やロール名、ラウンド数、ラウンドバフ列は折り返さず、必要時は省略表示するよう調整
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- ログバーぶんの右余白を調整
  - ログ本体は重なって表示してもよい前提のまま、常時見えているログバーだけは各ブロックに重ならないよう調整
  - プレイヤーブロック、場ブロック、手札ブロックの右幅をログバー分だけ短縮
  - `apps/web/src/styles.css`

- プレイヤーブロックの固定高を微調整
  - ステータス数値の下端が切れないよう、プレイヤーブロックの固定高を少し拡張
  - `apps/web/src/styles.css`

- 固定バトル画面の場・手札・ログ表示を再調整
  - 場と手札のカード列の高さを、カード表示と横スクロールバーに合わせた固定高へ調整
  - 場ブロックが縦に伸びすぎる問題と、手札カード下端が切れやすい問題を修正
  - ターンログは通常時非表示にし、右端の `ログ` バーへホバーしたときだけ固定高のドロワーとして展開する形へ調整
  - 展開したログはバトル画面の縦幅に合わせた固定高のまま、内部だけスクロールするように修正
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- 誘発と追加発動の用語を整理
  - 何かの効果で自分自身が追加で動くケースを、ログと表示文言で `誘発` として扱うよう調整
  - 他のカードを動かすケースは従来どおり `追加発動` のまま維持
  - `静電気` と `通電ショック` のカードテキストも新しい用語へ合わせて更新
  - `packages/engine/src/index.ts`
  - `packages/shared/data/cards/thunder_static.json`
  - `packages/shared/data/cards/thunder_shock.json`

- 固定バトル画面を `1080x880` に再調整
  - 固定バトル面のサイズを `1080x1080` から `1080x880` に変更
  - 上段、場、手札の高さ配分とカードサイズを縮小し、新しい固定サイズに収まるよう再配置
  - ターンログは非ホバー時に見えない透明トリガー型ドロワーへ変更し、右端ホバー時のみ展開する形に調整
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過

## 2026-05-21
- バトル画面を 1080x1080 固定レイアウトへ再構成
  - バトル中は左上ロゴを非表示にし、設定 / カード一覧 / ヘルプを固定バトル面の右上へ移動
  - 上段を `ステータス + ラウンド操作` の2カラムに組み替え、ラウンド操作パネルは発動中カードの画像と効果を中心に表示する構成へ変更
  - ターンログは右端の折りたたみドロワー化し、ホバー中だけ場や手札の上に重なるように表示
  - ステータス、場、手札を `1080x1080` の固定バトル面へ収めるようレイアウトを再配置
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- `つむじ風` の可変数値表示を修正
  - `連結している1枚につき` の `1` は固定値のままにして、変動対象は `+20` 側だけになるようテキストバインド位置を修正
  - 実処理ではなく表示側の不整合を修正
  - `packages/shared/data/cards/wind_tsumujikaze.json`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- マリガン時のログ順を調整
  - `手札を7枚まで補充 -> 〇枚を交換 -> 手札を7枚まで補充` の順でログが並ぶように、交換ログを再補充前へ移動
  - `packages/engine/src/index.ts`
  - `packages/engine/src/index.test.ts`
  - `npm test` を通過
  - `npm run typecheck` を通過
  - `npm run build` を通過

- ターンログのシステム系表示を分離
  - `ゲーム開始` や `ターン開始` などのゲーム進行ログを、オレンジ背景の一行ブロック表示へ変更
  - `マリガン` や `ラウンドバフ提示/選択` などの共通操作ログを、別色背景の一行ブロック表示へ変更
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- ターンログの外側グループ表示を再調整
  - 外側グループの青い棒を廃止し、左端だけ青く見える背景面へ変更
  - `〜の発動が終わりました。` ログはユーザー表示から除外
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- ターンログの同一カードグループ表示を強調
  - 同一カードによる複数ログを包む外側グループに、背景ブロックを追加
  - 内側の各ログブロックとは別に、まとまりが見える二段構造へ調整
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過

- ターンログの内部キー表示を非表示化
  - `CARD_EFFECT_APPLIED` などの内部ログコードはユーザー向けログから外し、本文だけを表示する形に調整
  - `apps/web/src/App.tsx`
  - `npm run typecheck` を通過
  - `npm run build` を通過

## 2026-05-19
- 右上に設定ボタンを追加し、カード発動音と音量設定を実装
  - `Web Audio API` でカード発動時の短い効果音を追加
  - ラウンド中のカード発動回数に応じて音階が上がるよう調整
  - 属性ごとに波形を少し変えて音色差を追加
  - 右上に `設定` ボタンを追加し、音量スライダーをモーダルで調整できるよう実装
  - 音量設定は `localStorage` に保存
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を通過
  - `npm run build` を通過
  - `npm test` を通過

## 2026-05-19
- 連続ダメージ中の追加発動タイミングを調整
  - `ActivationTask` に `isAdditional` を追加
  - ダメージ後の `after_damage_dealt` で積まれた追加発動は、親カードの全ヒット終了後にまとめて処理するのではなく、そのヒット直後に追加発動タスクだけ割り込ませるよう変更
  - これにより `電光石火` のような連続攻撃中でも、`通電ショック` が各ヒットごとに順次発動してログ順が分かりやすくなるよう調整
  - `packages/engine/src/index.ts`
  - `packages/engine/src/index.test.ts`
  - `npm test` を通過
  - `npm run typecheck` を通過
  - `npm run build` を通過

## 2026-05-19
- カード説明の数値差し替えを `effect定義 + 明示タグ` ベースへ移行開始
  - `CardDefinition` に `textValueBindings` を追加
  - ホバーポップアップのカード説明は、文面中の全数字を機械的に差し替える方式をやめ、明示的にバインドされた値だけ赤字で補正表示するよう変更
  - これにより `1個右側` や `1/5`、`5回` のような参照値・回数値を誤って数値変動扱いしにくく調整
  - まずは主要カードにバインドを追加
    - `none_punch`
    - `none_hadou`
    - `none_kintore`
    - `none_hybrid`
    - `wind_charge`
    - `wind_kamaitachi`
    - `wind_air_slash`
    - `wind_kazematoi`
    - `wind_tsumujikaze`
  - `packages/shared/src/types.ts`
  - `apps/web/src/App.tsx`
  - `packages/shared/data/cards/*.json`
  - `npm test` を通過
  - `npm run typecheck` を通過
  - `npm run build` を通過

## 2026-05-19
- ラウンド終了時効果の処理順を変更
  - 最後のカード発動後に、まず場の無効化を解除してから `at_round_end` を処理するよう変更
  - これにより、無効化されていたカードでも通常のラウンド終了時効果が発動するよう調整
  - `隙間風` で無効化されたカードは、即時発動分に加えて本来のラウンド終了時効果も発動するため、そのラウンド中に2回発動する挙動へ変更
  - `applyRoundPlan` も、無効カードで現在カードが取れない場合に最後まで解決が進むよう補正
  - `packages/engine/src/index.ts`
  - `packages/engine/src/index.test.ts`
  - `npm test` を通過
  - `npm run typecheck` を通過
  - `npm run build` を通過

## 2026-05-19
- 風属性の新カード `隙間風` と `つむじ風` を追加
  - `隙間風`
    - 右側カードの無効化後、その無効化されたカード群のラウンド終了時効果を即時発動する consume 効果を実装
    - engine に `trigger_round_end_effects_of_last_invalidated_cards` を追加
  - `つむじ風`
    - 発動で基礎ステータス `+10`
    - 連結している風属性カード1枚ごとにカード数値 `+20` する効果を実装
    - engine に `add_self_numeric_counter_per_connected_attribute_count` を追加
  - `packages/shared/data/cards/wind_sukimakaze.json`
  - `packages/shared/data/cards/wind_tsumujikaze.json`
  - `packages/shared/src/data.ts`
  - `packages/shared/src/types.ts`
  - `packages/engine/src/index.ts`
  - `packages/engine/src/index.test.ts`
  - `npm test` を通過
  - `npm run typecheck` を通過
  - `npm run build` を通過

## 2026-05-18
- `roundTriggerLimit` の内部カウンターがカード数値へ混ざる不具合を修正
  - `round_triggered_*` を engine 側のカード数値計算から除外
  - UI 側のカードテキスト補正表示でも同じ内部カウンターを除外
  - `エレクトリック` の見かけ上の `+1` と、`通電ショック` の実値汚染を解消
  - `packages/engine/src/index.ts`
  - `apps/web/src/App.tsx`
  - `npm test` を通過
  - `npm run typecheck` を通過
  - `npm run build` を通過

## 2026-05-18
- `スライム` の融合仕様を修正
  - 隣接する `スライム` 同士が融合したとき、カウンター同士ではなく元のカード数値も含めて掛け合わせるように変更
  - 例として `+20` と `+20` の融合後は `+400` として扱うよう engine を修正
  - カードテキストも現在の仕様に合わせて更新
  - `packages/shared/data/cards/water_slime.json`
  - `packages/engine/src/index.ts`
  - `packages/engine/src/index.test.ts`
  - `npm test` を通過
  - `npm run typecheck` を通過
  - `npm run build` を通過

## 2026-05-18
- 風属性カード 3 枚を追加
  - `かまいたち`
    - 発動で `基礎攻撃+10 -> ダメージ`
    - ラウンド終了時にカード数値 `+30`
  - `リワインド`
    - 前のラウンドで最後に発動したカードの効果を、このカードの効果として扱う処理を engine に追加
    - 前ラウンド参照がないときは空振りし、自己参照ループは残さないように調整
  - `エアスラッシュ`
    - 発動で `基礎魔法+20 -> ダメージ`
    - ラウンド終了時にカード数値 `+20`
  - `packages/shared/data/cards/wind_kamaitachi.json`
  - `packages/shared/data/cards/wind_rewind.json`
  - `packages/shared/data/cards/wind_air_slash.json`
  - `packages/shared/src/data.ts`
  - `packages/shared/src/types.ts`
  - `packages/engine/src/index.ts`
  - `packages/engine/src/index.test.ts`
  - `npm test` を通過
  - `npm run typecheck` を通過
  - `npm run build` を通過

## 2026-05-18
- 連結フレームがカード位置をずらさないよう調整
  - `connected-field-group` の内側余白をなくし、連結フレームの発光や枠線だけを外側へ逃がす形へ変更
  - 連結中でもカードの実際の配置位置が動かないように調整
  - `apps/web/src/styles.css`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-17
- `バブルブリンク` / `ドルフィン` の配置をドロップ時即確定へ変更
  - 配置用トレイから場へカードを置いた時点で確定扱いに変更し、確定ボタンを廃止
  - `バブルブリンク` は置いたトークンがその場でフィールドへ見えるようにし、全配置完了で自動発動する形へ変更
  - `ドルフィン` は複製カードを場へ持っていった瞬間に複製処理を完了し、トレイから消えるように変更
  - `apps/web/src/App.tsx`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-17
- 一時トレイから場へ置くときの配置判定を手札配置と同じ仕様へ統一
  - `バブルブリンク` / `ドルフィン` の一時トレイから場へドラッグするとき、場全体の `dragover` とカード左右判定が競合して配置位置がぶれる問題を修正
  - 右端の空白に入ったときだけ末尾配置へ切り替え、それ以外はカード上の左右半分判定を優先する形へ統一
  - `apps/web/src/App.tsx`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-17
- `バブルブリンク` / `ドルフィン` の配置 UI を一時トレイ表示へ再調整
  - 通常レイアウトには入れず、ステータスブロックに重なる absolute 配置の一時トレイへ変更
  - 一時トレイには「今から置くカード」だけを並べ、実際の配置プレビューは下の場に出す構成へ変更
  - `ドルフィン` は複製元カードを場から選んだあと、複製カードだけが一時トレイに出る流れへ調整
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-17
- `バブルブリンク` と `ドルフィン` の配置 UI を場の上側オーバーレイへ変更
  - 全画面ポップアップをやめて、自分の場フィールドの上にだけ配置用 UI が出る形へ変更
  - `ドルフィン` は、先に場のカードをクリックして複製元を選び、その後に複製カードだけをドラッグ配置する流れへ調整
  - `バブルブリンク` も同様に、場の上側だけで配置が完結する表示へ変更
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-17
- 役職一覧の並びを6枚基準の固定サイズへ調整
  - 役職選択画面の一覧は、6枚並んだときのカードサイズを基準に固定
  - 7枚目以降はカードサイズを変えずに下段へ折り返す構成へ変更
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-17
- 新ロール `ドルフィン` / `ブレイズ` を追加
  - `packages/shared/data/roles/role_dolphin.json` と `role_blaze.json` を追加し、ロール一覧へ反映
  - `ドルフィン`
    - 水属性の出現度 `+2`
    - 場の水カード3枚ごとに、ラウンド開始時の一時ステータスを `×2`
    - 最終攻撃の代わりに、場のカード1枚を選んで複製し、好きな位置に置けるよう engine / UI を追加
  - `ブレイズ`
    - 炎属性の出現度 `+2`
    - そのラウンド中に破壊されたカード枚数ぶん、最終攻撃前の一時ステータスを `×5`
  - `packages/engine/src/index.ts`
  - `packages/engine/src/index.test.ts`
  - `packages/shared/src/data.ts`
  - `packages/shared/src/types.ts`
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm test` を実行
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-17
- 連続ダメージ時の誘発挙動を回帰テストで固定
  - `電光石火` のような複数回ダメージを与えるカードがあるとき、`通電ショック` のような `after_damage_dealt` 誘発が各ヒットごとに走ることを確認
  - 非 `通電ショック` カードの連続ヒットでは、ヒット数ぶん追加発動キューが積まれることを engine テストで固定
  - `packages/engine/src/index.test.ts`
  - `npm test` を実行

## 2026-05-17
- カード前景イラストを一時非表示化
  - 追加したカード用画像が透過素材ではなかったため、前景イラストの描画だけ停止
  - 画像アセット自体は残し、後から透過素材へ差し替えたときに再利用できる状態を維持
  - `apps/web/src/App.tsx`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-16
- 配置フェーズのドラッグ導線を調整
  - そのラウンド中に手札から配置したカードは、場から手札エリアへドラッグして戻せるように変更
  - 場のカードに出していた `戻す` ボタンを廃止し、配置中カードの補助文言だけ残す形へ整理
  - 場の既存カードを右側へ並べ替えるとき、元位置の影響で `A B(設置元) B(カーソル) C D -> A C B D` のようにずれる不具合を修正
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-16
- カード前景イラストの重ね表示を追加
  - `カード画像` に追加された透過 PNG を `apps/web/src/assets/card-illustrations` へ移動し、カード定義 ID ベースの名前へ整理
  - 雷カードの前景イラストを、属性背景の上半分寄りに重ねるレイヤーとして追加
  - 場・手札・配置プレビュー・トークン配置・カード一覧・カード説明ポップアップで同じ前景イラストを表示するよう統一
  - 空になった `カード画像` ディレクトリを削除
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `apps/web/src/assets/card-illustrations/*`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-16
- 縦長カード背景への差し替えとカード比率の再設計
  - `tate_card_backgrounds` に追加された背景画像を `apps/web/src/assets/card-backgrounds` へ移動し、無属性用 `normal.png` は `none.png` に統一
  - 新たに `earth.png` と `dark.png` も読み込み対象へ追加し、属性ごとの背景参照を整理
  - ゲーム中のカード表示比率をすべて `縦3:横2` に統一し、場・手札・プレビュー・カード一覧のレイアウトを縦長前提へ調整
  - 背景がペールグレー基調になったため、カード上の文字色を黒系へ変更し、オーバーレイも明るい見え方に再調整
  - 空になった `tate_card_backgrounds` ディレクトリを削除
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `apps/web/src/assets/card-backgrounds/*`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-15
- 役職選択画面を詳細 + 一覧構成へ再編
  - 上段に選択中ロールの詳細、下段にフィルタ付きロール一覧を置く構成へ変更
  - 一覧には `すべて / 攻撃寄り / 魔法寄り / 制限あり / 制限なし` のフィルタを追加
  - ロール数が増えても比較しやすいよう、選択中ロールの説明・初期ステータス・制限を上段へ固定
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-15
- ステータス左上のロール表示を調整
  - ステータスヘッダ左上のロール名をプレイヤー名と同サイズで表示するように変更
  - ロール名にカーソルを合わせると、能力説明・初期ステータス・制限を確認できるポップアップを追加
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-15
- カード本文の補正後数値表示を追加
  - 場のカードに数値補正が乗っている場合、ホバーポップアップ内のカードテキストにある数値を補正後の値へ差し替え、変更された数値だけ赤字で表示するようにした
  - カードカタログの元定義表示はそのままにし、実カードインスタンスの `counters` / `numericValueMultiplier` だけを反映する形に整理
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-15
- `静電気` と `通電ショック` の雷連鎖仕様を再整理
  - `静電気` のカードテキストを、実装済みの「自分の場の『能力』カードが発動したとき」に合わせて更新
  - `通電ショック` は「他の通電ショックがダメージを与えたときだけ1回制限を意識する」解釈へ修正し、他カードのダメージには引き続き反応する定義へ戻した
  - engine 回帰テストを更新し、他カードダメージからの連鎖は継続しつつ、通電ショック同士で無限ループしないことを確認する形に調整
  - `packages/shared/data/cards/thunder_static.json`
  - `packages/shared/data/cards/thunder_shock.json`
  - `packages/engine/src/index.test.ts`
  - `npm test` を実行
  - `npm run typecheck` を実行
  - `npm run build` を実行

## 2026-05-15
- 雷属性カードと水属性トークン配置、［バランス］の仕様を修正
  - `静電気` を「自分の場の能力カードが発動したとき」に変更
  - `通電ショック` を「他の通電ショックがダメージを与えたとき」に変更
  - `バブルブリンク` の `chosen_positions` を使う専用配置 UI を追加し、ポップアップ内で `バブル` 2 個をドラッグ配置して確定できるようにした
  - ［バランス］は攻撃/魔法カードを配置可能にし、ラウンド開始後に無効化する形へ変更
  - `packages/shared/src/types.ts`
  - `packages/shared/data/cards/thunder_static.json`
  - `packages/shared/data/cards/thunder_shock.json`
  - `packages/shared/data/roles/role_balance.json`
  - `packages/engine/src/index.ts`
  - `packages/engine/src/index.test.ts`
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` は通過
  - `npm test` は通過
  - `npm run build` は通過

## 2026-05-15
- 場の並べ替え中の移動元表示を調整
  - 場カードをドラッグ中、元位置に実カード画像が残ってプレビューと二重に見えていたため、移動元はプレースホルダー表示に変更した
  - これにより、並べ替え時の配置位置が視覚的にずれにくいようにした
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` は通過
  - `npm run build` は通過

## 2026-05-15
- 属性ドロー乱数の偏りバグを修正
  - `drawSequence` ごとに RNG を作り直して最初の1回だけ使っていたため、似たシードで同属性に張り付きやすくなっていた
  - ドロー系列の RNG を進めて使う形に変え、同じ補充中に同属性へ極端固定しにくいよう修正した
  - `packages/engine/src/index.ts`
  - `packages/engine/src/index.test.ts`
  - `npm run typecheck` は通過
  - `npm test` は通過
  - `npm run build` は通過

## 2026-05-14
- ステータス欄に属性出現率ツールチップを追加
  - ラウンド表記の左にアイコンを置き、ホバーで現在の属性ごとの出現率一覧を表示するようにした
  - 山札残数と現在の属性重みから、その場で確率を再計算する形にした
  - `apps/web/src/App.tsx`
  - `apps/web/src/styles.css`
  - `npm run typecheck` は通過
  - `npm run build` は通過

## 2026-05-14
- 配置中のカード比率ぶれを修正
  - 配置プレビューの有無で場のカードの縦比率がわずかに変わって見える問題に対応。
  - カード行を上揃えにし、カード自体の高さを固定して、プレビュー表示中も比率がぶれないようにした。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-14
- 配置位置の判定をカード左右ベースへ変更
  - 場に置かれているカードの左半分 / 右半分へカーソルを持っていくことで、その左右どちらへ置くか決まるようにした。
  - カード間の専用ドロップスロットは撤去し、プレビューカードだけで差し込み位置を示す形へ整理した。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-14
- ソロモード表記とステータス上部レイアウトを調整
  - `ローカル検証モード` を `ソロモード` に変更。
  - プレイヤー表示名を `ローカルプレイヤー` から `プレイヤー` に変更。
  - ステータス上部を、左上にプレイヤー名とロール名、右上にラウンド数、中央に `今後実装予定` のラウンドバフ枠を置く構成へ変更。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-14
- ログとラウンド操作パネルのレイアウトを調整
  - ログパネルを最初から固定高にして、表示件数が少ないときでも縦幅が変わらないようにした。
  - ラウンド操作パネルを右カラムのログ下へ移動。
  - ラウンド操作内の説明を少し短くし、配置上限や制限情報はチップ表示へまとめてコンパクト化した。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-14
- 無効カードの表示と進行を調整
  - 無効になったカードをグレースケールと斜線オーバーレイで見分けやすくした。
  - 無効カードは現在の発動カード候補から外し、対象選択フェーズが出ないようにした。
  - ただし再発動のために一時的に捨て札や除外へいる `consume` カードは、必要なときだけ現在カードとして拾えるように維持した。
- テストを追加
  - 無効化されたカードが対象選択待ちの対象にならず、そのままスキップされることを確認するテストを追加。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm test` は 19 件成功。
  - `npm run build` は成功。

## 2026-05-14
- 小数倍率ダメージ計算の不具合を修正
  - `0.5` や `0.2` のような小数倍率を内部で先に切り捨てていたため、`オーバーチャージ` や `電光石火` のダメージが 0 になる問題を修正。
  - 倍率値の切り捨てはやめて、最終ダメージ確定時にのみ `floor` するようにした。
- テストを追加
  - `パンチ + オーバーチャージ` でカードダメージが 50 になることを確認するテストを追加。
  - `電光石火` の 1/5 ダメージが 5 回とも 0 にならないことを確認するテストを追加。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm test` は 18 件成功。
  - `npm run build` は成功。

## 2026-05-14
- 配置時のカード間隔と差し込み判定を調整
  - 場のカード同士のデフォルト間隔をかなり詰めた。
  - 場カードの左右へ置くときの差し込み判定幅を広げ、引っかかりやすくした。
- 確認結果
  - `npm run build` は成功。
  - `npm run typecheck` は成功。

## 2026-05-14
- 設置効果の自己反応を共通ルールで停止
  - `placed` 効果は、自分自身の発動前後や自分自身が与えるダメージの前後には反応しないよう、engine 側の共通判定を追加。
  - `ボルテックス` 以外の設置カードにも同じルールが適用されるようにした。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm test` は 16 件成功。
  - `npm run build` は成功。

## 2026-05-14
- `ボルテックス` の自己反応を調整
  - `ボルテックス` 自身の発動後には設置効果が反応しないように変更。
  - 他の自分のカードが発動したときだけ、左端カードの追加発動が起きるようにした。
- テストを追加
  - `ハイブリッド + ボルテックス` で、`ボルテックス` 自身の発動では追加発動が増えないことを確認するテストを追加。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm test` は 16 件成功。
  - `npm run build` は成功。

## 2026-05-14
- 配置時のドロップ挙動とカード比率を調整
  - 場の上でカードを離したとき、現在表示されているプレビュー位置へそのまま配置されるようにした。
  - 場のカード同士の間隔を少し詰めた。
  - カード表示の比率を `縦2:横3` に固定した。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-14
- ドラッグ開始処理を安定化
  - カードをつかめなくなっていたため、`dragstart` 時に `dataTransfer` を明示設定するようにした。
  - ドラッグ開始直後のプレビュー位置更新は次フレームへずらし、つかんだ瞬間にドラッグが途切れにくいようにした。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-14
- 配置UIのドロップ領域を修正
  - 配置プレビュー導入後にドラッグしづらくなっていたため、差し込み判定の幅を広げた。
  - 場のカード幅と場エリアの高さを固定し、配置中にサイズがぶれにくいようにした。
  - 場の右側の空白へドロップしたときは、自動的に現在の右端へ追加されるようにした。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-14
- 配置中のプレビュー表示を調整
  - 場へカードを配置するときの青い差し込み線と、右端の `ここに配置` 表示を削除。
  - 代わりに、ドラッグ中カードの半透明プレビューを配置予定位置へ表示するようにした。
  - 既存の場カードをドラッグして並べ替える場合も、実カードではなくプレビュー位置で差し込み先が分かるようにした。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-14
- 追加発動ログを改善
  - 追加発動が起きたとき、どのカードのどの種類の効果で追加発動したか分かるログを出すようにした。
  - 追加発動先のカード名も一緒に出し、連鎖の流れを追いやすくした。
- テストを追加
  - `ボルテックス` による左端追加発動で、発生元と対象先がログに出ることを確認するテストを追加。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm test` は 15 件成功。
  - `npm run build` は成功。

## 2026-05-14
- 設置効果の常時オーラ挙動を整理
  - `apply_enchant_to_all_ally_field_cards` を、単発発動ではなく「場にある限り維持される常時オーラ」として同期する処理へ変更。
  - 同じ設置カード 1 枚から同じ対象へは、同じエンチャントを 1 つだけ維持するようにした。
  - 同種の設置カードが複数ある場合は、発行元ごとに別エンチャントとして重なるようにした。
  - 設置元カードが場を離れたときは、そのカード由来の常時エンチャントが自動で消えるようにした。
- エンチャント保持情報を拡張
  - エンチャントに「どの設置カード由来か」を持たせ、発行元単位で重複管理できるようにした。
- テストを追加
  - 単一の設置元からは同じ対象へ 1 つだけエンチャントが付くことを確認するテストを追加。
  - 設置元が破壊されて場を離れたとき、常時エンチャントが消えることを確認するテストを追加。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm test` は 14 件成功。
  - `npm run build` は成功。

## 2026-05-14
- 氷属性の常時エンチャント挙動を修正
  - `on_enter_field` の判定を見直し、氷属性の「場のカードすべてにエンチャント付与」がラウンド途中に生成されたカードにも追従するようにした。
  - 自分より前に入場していたカードへ後から重複反応しないよう、入場順も加味して処理するようにした。
- 全体対象ログと表示文言を整理
  - 場全体へのエンチャント付与は、対象カードごとの連続ログではなく、まとめた 1 行ログで出すようにした。
  - ステータス増減ログは、カード効果による変動か、付与エンチャントによる変動かを文言で区別するようにした。
  - `RESOLUTION_STARTED` の表示文言を `ターン開始` に変更した。
- テストを追加
  - 氷属性の常時エンチャントが後から生成されたカードにも付くことを確認するテストを追加。
  - 場全体対象ログが要約されることを確認するテストを追加。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm test` は 12 件成功。
  - `npm run build` は成功。

## 2026-05-13
- カードフレームとフローティングテキスト位置を再調整
  - 選択中カードと発動中カードのフレーム色をさらに濃くし、透明度も下げて視認性を強化。
  - カード効果とエンチャント効果のフローティングテキストが同時に出るとき、左右にずらして重なりにくくした。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-13
- カードホバーポップアップの背景画像を削除
  - `hover-tooltip` に残っていたカード背景レイヤーを無効化し、黒無地のポップアップに修正。
- 確認結果
  - `npm run build` は成功。
  - `npm run typecheck` は成功。

## 2026-05-13
- カードフレーム色を調整
  - 選択中カードのフレーム色を暖色寄りへ変更し、操作対象であることが見やすいようにした。
  - 発動中カードのフレーム色を寒色寄りへ変更し、進行中カードだと分かりやすいようにした。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-13
- カード側フローティングテキストの表示内容を調整
  - 生ログ文ではなく、カードによる数値差分のみを表示するよう変更。
  - 通常のカード効果による数値変動は赤、エンチャント由来の数値変動は紫で出し分けるようにした。
  - エンジン側の差分ログに数値差分メタ情報を追加し、UI はそのメタ情報を使ってフローティングテキストを描画するよう変更。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-13
- ログパネル周辺の余白を調整
  - 右カラムのログパネルが左カラムの高さに引っ張られないようにし、内容量に応じた高さで止まるようレイアウトを修正。
- 確認結果
  - `npm run build` は成功。
  - `npm run typecheck` は成功。

## 2026-05-13
- ステータス UI とフローティングテキストを追加
  - 上部6ブロックを 3 列 2 段に並べ替え、左列を基礎攻撃 / 一時攻撃、中央列を基礎魔法 / 一時魔法、右列をラウンド得点 / 合計得点に整理。
  - `STATUS_CHANGED`、`DAMAGE_DEALT`、`FINAL_ATTACK` を使って、上部ステータスブロックから赤いフローティングテキストが出るようにした。
  - 発動カードからもフローティングテキストを出すようにし、通常効果は赤、エンチャントや追加補正系は紫で出し分けるようにした。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-13
- カード選択 UI を調整
  - 選択中カードのフレームを二色表示に変更し、左上と右下で別カラーになるよう調整。
  - カードホバーポップアップの背景を黒へ変更。
- 再発動と生成カードの解決順を調整
  - consume カードが再発動対象になったとき、discard / removed から一時的に場へ戻して再発動し、その後に再び consume 後処理へ入るよう修正。
  - `create_token` で現在の発動位置より後ろに生成されたカードは、そのラウンド中の未解決キューへ差し込まれて発動するよう修正。
- テスト追加
  - consume カードの一時復帰再発動テストを追加。
  - `ドロップアウト -> アクア` の後続発動テストを強化。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm test` は 10 件成功。
  - `npm run build` は成功。

## 2026-05-13
- ラウンド終了時の捨て札選択 UI を変更
  - 場から捨てるカードを、専用ボタンではなくカード自体のクリックで選ぶ方式に変更。
  - 2枚までの選択上限をクリック時点で守るようにし、選択中カードは強調表示されるようにした。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-11
- 雷カードの自己連鎖条件を調整
  - `静電気` を「このカード以外の自分のカードが発動したとき」に反応するよう修正。
  - `通電ショック` を「このカード以外の自分のカードがダメージを与えたとき」に反応するよう修正。
  - エンジンに「発動元カードがこのカード自身ではない」条件を追加し、自己連鎖しないことをテストで固定した。
- テスト整理
  - `packages/engine/src/index.test.ts` を UTF-8 で整理し直し、雷カードの回帰テストを追加。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm test` は 9 件成功。
  - `npm run build` は成功。

## 2026-05-10
- カード一覧ポップアップを追加
  - 右上のヘルプボタン横に、カード一覧を開くボタンを追加。
  - モーダル内で全カードを一覧表示し、名前・説明・種別・属性・デッキ入り可否を見比べられるようにした。
  - 検索、種別フィルタ、属性フィルタを追加して、目的のカードを探しやすくした。
- 確認結果
  - `npm run typecheck` は成功。
  - `npm run build` は成功。

## 2026-05-08
- 配置フェーズの UX を改善。
  - 場へカードをドラッグ中、スクロール領域の左右端へ寄せるとその方向へ自動スクロールするようにした。
  - 横に長い場でも、ドラッグしたまま奥の配置位置へ届くように調整。
- 確認結果。
  - `npm run typecheck` は通過。
  - `npm run build` は通過。

- ヘルプ導線を変更。
  - 常設のヘルプパネルを廃止し、右上の `？` ボタンから開くモーダル方式へ変更。
  - 必要なときだけ操作説明と用語説明を見られるようにした。
- 確認結果。
  - `npm run typecheck` は通過。
  - `npm run build` は通過。

- 役職選択 UI を改善。
  - 開始前の役職選択をプルダウンから一覧カード方式へ変更。
  - 役職ごとの初期ステータス、能力説明、使用制限をその場で見比べられるようにした。
- 確認結果。
  - `npm run typecheck` は通過。
  - `npm run build` は通過。

- 簡易パーティクル演出を追加。
  - `ReplayEvent` に反応する図形ベースの演出レイヤーを `apps/web/src/App.tsx` と `styles.css` に追加。
  - `CARD_ACTIVATED` / `DAMAGE_DEALT` / `FINAL_ATTACK` に合わせて、属性ごとの円・菱形・線・リングなどを表示するようにした。
  - 発動カード位置を基準に出し、最終攻撃はステータス付近も使う軽量な演出方式にした。
- 確認結果。
  - `npm run typecheck` は通過。
  - `npm run build` は通過。

- UI 表示文言を調整。
  - カードの種別と属性を、表示上は英語ではなく日本語ラベルで出すように変更。
- 確認結果。
  - `npm run typecheck` は通過。
  - `npm run build` は通過。

- `patch_notes_public.md` の公開向け方針を整理。
  - 内部実装やデータ管理だけの記述を外し、プレイや見た目に影響する内容だけを残す形へ再編集。
  - 項目も `新カード追加` / `仕様変更` / `UI変更` / `ログ調整` など、外部プレイヤー向けの表現へ寄せた。

- JSON バンドル参照資料の再配置を実施。
  - `choujigen_battle_card_json_bundle` を、資料用の `docs/json_bundle_reference` へ再配置。
  - 残っていた重複カード JSON とロール JSON、付属 README を `docs/json_bundle_reference/cards` / `roles` へ整理。
  - 空になった `choujigen_battle_card_json_bundle` を削除。
  - 空になった `pixel_card_backgrounds` も削除。

- 属性別カード背景画像を配置整理。
  - `pixel_card_backgrounds` に置かれていた画像を `apps/web/src/assets/card-backgrounds` へ移動。
  - 現行属性名に合わせて `void.png` は `none.png` へ変更。
- カード UI に背景画像を適用。
  - 場カード、手札カード、ホバーポップアップで属性ごとの背景画像を使うよう変更。
  - 選択中 / 発動中の強調表示は、背景画像を消さずに重ねて見せる方式へ調整。
- 確認結果。
  - `npm run typecheck` は通過。
  - `npm run build` は通過。

- 全カード対応に向けて共有型を拡張。
  - `trigger` / `condition` / `operation` の型定義を、追加カード JSON が使っている仕様に合わせて拡張。
  - `PlayerState` に次ラウンド補充枚数や次ラウンド基礎強化の予約値を追加。
- カードカタログ読込を拡張。
  - `packages/shared/src/data.ts` で既存サンプルのみではなく、配置済みのカード JSON 一式を `sampleCards` に載せるよう変更。
  - エンチャント定義と、`create_token` で参照されるトークン定義は自動でデッキ対象外にするよう整理。
- `packages/engine/src/index.ts` をイベント駆動ベースの新エンジンへ差し替え。
  - 追加発動キュー
  - 場の `placed` トリガー
  - `before_card_activates` / `after_card_activates`
  - `before_damage_dealt` / `after_damage_dealt`
  - `on_enter_field` / `on_field_state_check`
  - `at_next_round_start` / `at_round_end`
  - `when_host_card_destroyed` / `when_ally_field_card_destroyed`
  - 連続属性補正
  - 生成、融合、無効化、復活予約、次ラウンド予約
  - 自己追加発動の無限ループ回避
- 回帰テストを追加。
  - `静電気` の自己追加発動
  - `ドロップアウト` のトークン生成
  - `夏の風鈴` の次ラウンド予約
  - `ブリザード` の自己エンチャント込みダメージ
- 確認結果。
  - `npm run typecheck` は通過。
  - `npm test` は 8 件通過。
  - `npm run build` は通過。

## 2026-05-05
- JSON バンドルの配置整理を実施。
  - `choujigen_battle_card_json_bundle/cards` の追加カード JSON を確認。
  - 既存と同名の 7 件は上書きせず、既存ファイルを優先して維持。
  - 新規 44 件のカード JSON を `packages/shared/data/cards` へ移動。
  - バンドル内のロール JSON は既存 3 件と同名だったため、移動せず据え置き。
- 補足。
  - 追加カードには現時点のエンジン未対応の operation も含まれるため、読込対象の拡張はまだ行っていない。
- `docs/12_run_flow.md` を追加。
  - 初回セットアップ
  - `typecheck` / `test` / `build`
  - `dev:web` によるローカル起動
  - 画面確認の手順
  - よく使う確認パターン
- `docs/00_README.md` に実行手順書への案内を追記。
- カード / ロール定義の保存形式を変更。
  - `packages/shared/src/data.ts` の直書き定義を JSON へ分離。
  - `packages/shared/data/cards/*.json`
  - `packages/shared/data/roles/*.json`
  - `sampleCards` / `sampleRoles` は JSON import から組み立てる形へ変更。
- 確認結果。
  - `npm run typecheck` は通過。
  - `npm test` は 4 件通過。
  - `npm run build` は通過。

## 2026-04-21
- 実装開始。`docs/` を読み、Phase 1 から順に進める方針を確定。
- 仕様すり合わせを実施。
  - 初回スコープは Phase 1 -> Phase 2 -> Phase 3 の最小実装。
  - `progress.md` と `patch_notes_public.md` は新規作成して運用。
  - デッキ構成は現状「全カード3枚ずつ」。
  - 破壊 / エンチャントは自分で選ぶ UI にする。
  - `deal_damage_*` と `FINAL_ATTACK` はどちらもスコア加算対象。
  - カード解決順は「無効・破壊確認 -> placed の解決前補正 -> 主効果 -> enchant -> 解決後トリガー -> consume 後処理」で固定。
- モノレポ構成の初期ファイルを追加。
- `apps/web` にローカル検証用 UI の土台を追加。
- `packages/shared` に型定義とサンプルカード / 役職定義を追加。
- `packages/engine` に最小エンジンを実装。
  - 5 ラウンド進行
  - マリガン
  - 配置
  - 左から順の解決
  - enchant の追加解決
  - consume の後処理
  - 最終攻撃
  - ラウンド終了時の 2 枚破棄
- `workers/room-worker` に DO + WebSocket 前提の骨組みを追加。
- 確認結果。
  - `npm install` を完了。
  - ワークスペース全体の `npm run typecheck` は通過。
  - ワークスペース全体の `npm run build` は通過。
  - `packages/engine` に Vitest 回帰テストを追加。
  - `npm test` で 4 件のテスト通過を確認。
  - UI にヘルプ欄を追加し、現在使っている主要用語を画面から確認できるようにした。

## 2026-04-22
- `patch_notes_public.md` の公開向け書式を整理。
  - `## vX.Y.Z (YYYY-MM-DD)` 形式の見出しへ統一。
  - 「入力フロー変更」「UI変更」「ログ改善」など、公開向けに読みやすいカテゴリ単位へ整理。
- 場と対象選択の UI を追加修正。
  - 場エリアが横に広がりすぎないよう、パネル内で横スクロールする構成へ調整。
  - 場カードを選ぶ効果は `select` ボックスではなく、場のカードを直接クリックして選ぶ方式へ変更。
- 表示文言とログを追加修正。
  - UI 表示用の文言から `解決` を外し、`発動` ベースの表現へ変更。
  - カードごとの効果で基礎攻撃 / 基礎魔法 / 一時攻撃 / 一時魔法がどれだけ増減したかをログへ出すようにした。
  - 破壊やエンチャント付与も、どのカードに何をしたか分かるログを追加。
- 入力と配置の挙動を追加修正。
  - 交換フェーズではカード自体をクリックして交換対象を選ぶ方式へ変更。
  - 交換確定後に交換フェーズへ戻ってしまう不具合を修正。
  - 場の既存カードもドラッグアンドドロップで再配置できるようにした。
  - 手札から場へ置いたカードは、配置フェーズ中の手札一覧から隠すようにした。
- 解決とログを追加修正。
  - 対象選択カードの発動時 UI が消えてしまう不具合を修正。
  - 最終攻撃ログに、どのステータスでダメージを与えたかを表示するようにした。
- 入力フローを段階制に変更。
  - `交換 -> 配置 -> 対象選択 -> 解決` の順で進むようにした。
  - 交換と配置のチェックボックスを廃止し、各段階で確定後に次の行動へ遷移するようにした。
  - 対象選択が必要なカードは、配置順に 1 枚ずつ順番に選ぶ UI に変更した。
- カード説明表示を変更。
  - 常時表示の詳細パネルを廃止。
  - カードにカーソルを合わせたときのポップアップ表示へ変更。
  - スクロール領域でポップアップが切れないよう、カード行の外に固定表示する方式へ修正。
- 配置と解決の流れをさらに変更。
  - 手札からドラッグアンドドロップで場の好きな位置へ差し込めるようにした。
  - 配置後は場の左から順に、数百ミリ秒単位で自動的に順次発動するようにした。
  - 対象選択が必要なカードは、そのカードが実際に発動するタイミングで選ぶ方式へ変更した。
- エンジンを段階解決に対応。
  - `startRoundResolution` / `getCurrentResolutionCard` / `getCurrentResolutionTargetKeys` / `resolveNextCard` を追加。
  - `applyRoundPlan` は内部的に逐次解決 API を使う形へ整理した。
- ログを改善。
  - 攻撃カード / 魔法カードのダメージ発生時にログへダメージ値を記録するようにした。
- エンジンに `applyMulliganOnly` を追加し、交換だけ先に確定できるようにした。
- 確認結果。
  - `npm run typecheck` は通過。
  - `npm test` は 4 件通過。
  - `npm run build` は通過。
## 2026-05-14
- 右上ヘッダーの操作群を整理
  - 右上は `カード一覧` と `ヘルプ` の2ボタンだけにして、ソロモード / ラウンド / フェーズなどの状態チップは外した。
- 確認
  - `npm run typecheck` を実行予定
  - `npm run build` を実行予定

## 2026-05-14
- 倍率系のステータス上昇ログ表記を調整
  - `multiply_*` 系や役職倍率補正のログは、`+` だけでなく `×1.10（+9）` のように倍率と実際の増加量を併記する形式へ変更。
  - フローティングテキスト用の `deltaTexts` は従来どおり加算差分だけを保持して、画面演出は崩さないよう調整。
- 確認
  - `npm run typecheck` は通過
  - `npm test` は 25 件通過
  - `npm run build` は通過

## 2026-05-14
- 新ロール `チャージ` を追加
  - `role_charge.json` を追加し、基礎攻撃30 / 基礎魔法30 の新ロールを sampleRoles に組み込み。
  - 毎ラウンド開始時に雷属性の出現度を1上げる処理と、自分の場のカード発動ごとに基礎ステータスを1.1倍する処理を engine に追加。
  - 手札補充は `drawAttributeWeights` を参照して、利用可能属性の基本重み1に追加重みを足した比率で属性抽選する形へ拡張。
  - engine テストに、`チャージ` の出現度蓄積、発動時倍率、雷属性が引かれやすくなるケースを追加。
- 確認
  - `npm test` は 25 件通過
  - `npm run typecheck` は通過
  - `npm run build` は通過

## 2026-05-14
- 手札補充を属性抽選ベースへ変更
  - 山札補充時は、まず山札に残っている属性の中から等確率で属性を選び、その属性の候補カードからランダムに1枚引く方式へ変更。
  - `PlayerState` に `drawSequence` を追加して、補充ごとの乱数系列を安定して追えるように調整。
  - engine テストに、山札の並び順ではなく属性抽選が先に働くことを確認するケースを追加。
- 確認
  - `npm test` は 22 件通過
  - `npm run typecheck` は通過
  - `npm run build` は通過

## 2026-05-14
- 自分の場対象の選択 UI を修正
  - 対象として選んだカードは、選択操作が完了した後も選択中フレームが残るように調整。
  - 単一対象のカードでは、一度選んだあとでも別の場カードをクリックして選び直せるよう修正。
- 確認
  - `npm run typecheck` は通過
  - `npm run build` は通過

## 2026-05-14
- 連結グループの可視化 UI を追加
  - 連結参照効果を持つカードが場にあるとき、engine と同じ連結判定で隣接グループをまとめて囲むフレームを追加。
  - 連結グループ枠は属性ごとの色と粒子感のある発光表現で、どのカード群が連結対象か見分けやすいように調整。
- 確認
  - `npm run typecheck` は通過
  - `npm run build` は通過

## 2026-05-14
- `エレクトリック` の設置効果もラウンド1回制限へ変更
  - `thunder_electric.json` に `roundTriggerLimit: 1` を追加し、右隣カードの追加発動はラウンド中1回だけ起きるよう調整。
  - engine テストに、`エレクトリック` が右隣カードを1回だけ追加発動させるケースを追加。
- 確認
  - `npm test` は 21 件通過
  - `npm run typecheck` は通過
  - `npm run build` は通過

## 2026-05-14
- `ボルテックス` の設置効果をラウンド1回制限へ変更
  - `EffectDefinition` に `roundTriggerLimit` を追加し、設置トリガー系の効果がラウンド中に指定回数だけ反応する仕組みを engine に実装。
  - `ボルテックス` は `after_card_activates` で最初の1回だけ左端カードを追加発動させる定義へ変更し、A→A(追加発動)→B→… の進行になるよう調整。
  - engine テストに、`ボルテックス` がラウンド中1回だけ反応する並び順ケースを追加。
- 確認
  - `npm run typecheck` は通過
  - `npm test` は 20 件通過
  - `npm run build` は通過

## 2026-05-14
- 配置済みカードのドラッグがすぐ切れる不具合を修正
  - 場カードのドラッグ開始と同時に元カードを描画ツリーから外していたためブラウザ側でドラッグ終了扱いになっていたので、元カードを場に残したまま並べ替えプレビューを出す形に修正。
- 確認
  - `npm run typecheck` は通過
  - `npm run build` は通過

## 2026-05-14
- `かげろう` 後に進行が止まる不具合を修正
  - 無効化で次の発動対象が一時的に存在しない場合でも、UI が自動で次の `resolveNextCard` を呼び直してラウンド処理を最後まで進めるよう調整。
- 確認
  - `npm run typecheck` は通過
  - `npm run build` は通過

## 2026-05-14
- 配置ドラッグ判定の競合を調整
  - 場全体のドラッグ判定が常に右端配置へ上書きしないようにして、右端の余白にカーソルがあるときだけ末尾配置プレビューへ切り替わるよう修正。
  - 既存の場カードを動かすとき、カード内ボタンがドラッグ開始を邪魔しにくいようにして、配置中カードのカーソル表示も掴みやすい見た目へ調整。
- 確認
  - `npm run typecheck` は通過
  - `npm run build` は通過
## 2026-05-19
- ターンログのグループ表現を左ライン型へ再調整
  - 外側のバナー方式をやめて、同一カードのログを左側の共通ラインで束ねる見た目に変更。
  - 内側の各ログは個別表示のまま残し、カード単位のまとまりだけを外側ラインで示す形に調整。
  - 確認: `npm run typecheck` / `npm run build` 通過

- ターンログをカード単位でグループ表示
  - 連続する同一カード由来のログをひとまとめにし、外側にカード単位のバナーを表示。
  - 内側には各効果やダメージ結果のログを並べる構成へ変更。
  - 確認: `npm run typecheck` / `npm run build` 通過

- ターンログ行の高さを固定
  - ログ数が少ないときに各ログが縦方向へ引き延ばされないよう、ログブロックの高さと各ログ行の高さを固定。
  - 確認: `npm run typecheck` / `npm run build` 通過

- カード説明文の改行整形を追加
  - `発動：`、`設置：`、`消費：` などの見出し前で表示上の改行を入れるように変更。
  - ホバー時のカード説明や一覧表示で、効果区切りが読みやすくなるよう調整。
  - 確認: `npm run typecheck` / `npm run build` 通過

- ラウンドバフ選択UIを中央ポップアップ化
  - ラウンドバフ選択を右側パネルから外し、画面中央のモーダル表示へ変更。
  - 3候補を横並びで表示し、ラウンドバフ選択画面として見やすく調整。
  - 狭い画面では1列に落ちるようレスポンシブ対応を追加。
  - 確認: `npm run typecheck` / `npm run build` 通過

- ラウンドバフ表示位置を調整
  - ステータスブロックのラウンドバフ表示を、プレイヤー名・ラウンド数と同じ段の中央へ移動。
  - 確認: `npm run typecheck` / `npm run build` 通過

- 数値表示フォーマットを調整
  - ステータス表示、カード文中の補正数値、フローティングテキストの数値表示を共通化。
  - 12桁までは通常表示、13桁以上は `e+` 表記へ切り替えるように変更。
  - 確認: `npm run typecheck` / `npm run build` 通過

- ラウンドバフ候補抽選を調整
  - 同一ラウンド中に提示される3つのラウンドバフ候補が重複しないように変更。
  - ラウンドバフ抽選テストにも、候補3枠がユニークであることを追加。
  - 確認: `npm test` / `npm run typecheck` 通過

- ラウンドバフ機能を実装
  - ラウンド終了後、3候補から1つ選ぶラウンドバフ選択を追加。
  - 各ラウンドで1回だけリロール可能に変更。
  - 候補は重複あり抽選にして、現状3種でもリロールが意味を持つ形へ調整。
- ラウンドバフ定義を追加
  - `sampleRoundBuffs` を shared に追加し、`ボルテッシモ`、`追い風ラッシュ`、`映しミラー` を実装。
  - `PlayerState.selectedRoundBuffs` と `LocalGameState.pendingRoundBuffChoice` / `roundBuffCatalog` を追加。
- エンジン調整
  - ラウンド終了処理を `捨て札選択 -> ラウンドバフ提示 -> バフ選択後に次ラウンド開始` の流れへ変更。
  - 雷/風の出現度補正を、既存の `drawAttributeWeights` とラウンドバフ効果の合算で計算するよう調整。
  - `ボルテッシモ` による雷カード数値+10%を、場のカードへの動的補正として同期。
  - `追い風ラッシュ` による風カードのラウンド終了時 `×1.3` を実装。
  - `映しミラー` は、ほかの獲得済みラウンドバフの効果数を複製する形で処理。
- UI調整
  - ステータス中央のラウンドバフ枠を実装し、獲得済みバフをアイコン表示。
  - アイコンホバーで説明ポップアップを表示。
  - ラウンド終了時の操作パネルに、ラウンドバフ選択UIとリロールUIを追加。
  - バフ画像未設定時はグレー背景の `Noimage` 表示に対応。
- 確認
  - `npm run typecheck` 通過
  - `npm test` 通過
  - `npm run build` 通過

## 2026-05-25
- `{{...}}` を変動数値の内部マーカーとして使えるように調整
  - カード文描画で `{{...}}` を解釈し、表示上は括弧を出さず背景プレート付きの値だけを表示
  - JSON テンプレートにも `{{...}}` の内部運用ルールを追記
  - `apps/web/src/App.tsx`
  - `docs/09_card_json_templates.md`
- 2026-05-25: バーンアウトとヘルフレイムの textValueBindings を撤去し、変動数値として扱わないよう調整。
- 2026-05-25: マリンベールの変動数値バインドを `5枚` ではなく `+5` 側へ修正。
- 2026-05-25: フェローファイアの変動数値指定を撤去し、ヘルフレイムの ×3 を変動数値指定へ復帰。
- 2026-05-25: エンチャント説明文の接頭辞を `エンチャント:` から `発動:` / `設置:` に統一。
## 2026-05-25
- 新カード8枚を追加し、関連する engine 操作を実装
  - 追加カード: `百花繚乱`、`匠のこだわり`、`凍結の呪い`、`凍傷`、`つらなるつらら`、`放射冷却`、`冷却反応`、`変電施設`
  - engine に `ランダム追加発動`、`直前属性比較による無効化`、`両隣エンチャント付与`、`連結エンチャント参照の基礎攻撃倍化`、`次ラウンド開始時の基礎ステータス減衰 + カード数値加算`、`非雷カードの雷化 + 数値補正` を追加
  - `匠のこだわり` は自分自身を無効化しないように調整
  - 永続オーラ系の型推論崩れを修正し、`apply_enchant_to_adjacent_cards` を常時同期対象へ追加
  - `npm test`、`npm run typecheck`、`npm run build` を確認
- `textValueBindings` に `writtenValueKind` / `affectsValueKind` を追加し、既存カードの変動数値にも順次付与した。
