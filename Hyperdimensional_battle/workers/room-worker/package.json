{
  "name": "@hyperdimensional-battle/room-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml --ip 0.0.0.0 --port 8787",
    "deploy": "wrangler deploy --config wrangler.toml",
    "typecheck": "tsc --noEmit"
  }
}

